<?php
declare(strict_types=1);

// Avatar SDK utility: feature_hook

class AvatarFeatureHook
{
    public static function call(AvatarContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
